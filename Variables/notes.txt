Variables 
    -> containers to store the values 
    -> used to refer memory location 
    -> also known as the identifier
    -> not need to declare Variable before using them
    ->  different variale name with same value will be store in same address 
    ->  same variale name with different value will be store in different address 

Keywords
  -> are the special reserved words that have a specific purpose and meanings

id() - built in function
All objects in python has its own unique id
It is represented by an integer values
id() function returns the id of the specified object  where it stored

